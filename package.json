{
  "name": "leadserve-oms-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Firestore security-rules unit tests for the LEADSERVE OMS shell (dev tooling only; not part of the hosted app).",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"node tests/rules.test.mjs\"",
    "test:provision": "firebase emulators:exec --only firestore \"node tests/provision.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0",
    "firebase-admin": "^12.3.0"
  }
}
